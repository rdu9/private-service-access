[Unit]
Description=Private tracker API
After=network.target

[Service]
User=<USER>
WorkingDirectory=/home/<USER>/tracker
ExecStart=/home/<USER>/tracker/.venv/bin/uvicorn app:app --host 127.0.0.1 --port 8001
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
